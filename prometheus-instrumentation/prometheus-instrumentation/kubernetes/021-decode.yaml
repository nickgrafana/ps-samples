---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: pi
  name: pi-decode
  labels:
    app: pi-decode
spec:
  selector:
    matchLabels:
      app: pi-decode
  replicas: 1
  template:
    metadata:
      labels:
        app: pi-decode
    spec:
      containers:
      - name: pi-decode
        image: nickolivera296/prometheus-instrumentation:latest
        env:
          - name: ENCODE_HOST
            value: "pi-encode.pi.svc.cluster.local:8080"
          - name: DECODE_HOST
            value: "pi-decode.pi.svc.cluster.local:8080"
          - name: DB_HOST
            value: "pi-db.pi.svc.cluster.local:8080"
        ports:
        - name: web
          containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: pi-decode
  name: pi-decode
  namespace: pi
spec:
  ports:
  - name: web
    port: 8080
    protocol: TCP
    targetPort: web
  selector:
    app: pi-decode
